# 每日代码扫描原始材料 2026-06-07

扫描时间: 2026-06-07 23:01:35
仓库总数: 122
有变更仓库数: 5


---
## 仓库: rrsjk-admin-web
路径: /data/pvcode/rrsjk-admin-web
分支: master
范围: 078ef9e85062786376adb82cc5c69680772b1845..08e82403941667b49156cea583f09b806c09d825

### Commit 和文件变更
```text
08e82403941667b49156cea583f09b806c09d825|yumiao|2026-06-07 16:01:48 +0800|fix:修复分中心对应人员的关系

M	src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java
M	src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java
3e80678be17889e317e826d680db2b9e64af6c25|yumiao|2026-06-07 15:45:58 +0800|fix:服务商管理分中心列表查询

M	src/main/java/com/rrsjk/admin/controller/light/LightSpController.java

```

### 变更统计
```text
 .../light/DispatchWorkOrderController.java         | 45 +++++++++++-----------
 .../admin/controller/light/LightSpController.java  | 33 ++++++++--------
 .../light/lightStationNannyUserController.java     |  6 ++-
 3 files changed, 44 insertions(+), 40 deletions(-)

```

### 关键业务文件 diff

### src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java
业务域候选: 订单
```diff
diff --git a/src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java b/src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java
index c23392b53..b7f558572 100644
--- a/src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java
+++ b/src/main/java/com/rrsjk/admin/controller/light/DispatchWorkOrderController.java
@@ -148,102 +148,103 @@ public class DispatchWorkOrderController extends AbstractController {
 
 		if (!Strings.isNullOrEmpty(criteria.getStationCode())) {
 			params.put("stationCode", criteria.getStationCode());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getStationName())) {
 			params.put("stationName", criteria.getStationName());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getStationStatus())) {
 			params.put("status", criteria.getStationStatus());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getWorkOrderNo())) {
 			params.put("workOrderNo", criteria.getWorkOrderNo());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getProjectDirector())) {
 			params.put("projectDirector", criteria.getProjectDirector());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getDispatchStatus())) {
 			params.put("dispatchStatus", criteria.getDispatchStatus());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getStartWorkStatus())) {
 			params.put("startWorkStatus", criteria.getStartWorkStatus());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getRoadWorkStatus())) {
 			params.put("roadWorkStatus", criteria.getRoadWorkStatus());
 		}
 		if (!Strings.isNullOrEmpty(criteria.getDocType())) {
 			params.put("docType", criteria.getDocType());
 		}
 		DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
 		if (!Strings.isNullOrEmpty(dispatchStartAt)) {
 			LocalDateTime startTime = LocalDateTime.parse(dispatchStartAt.trim() + " 00:00:00", formatter);
 			params.put("dispatchStartAt", startTime);
 		}
 		if (!Strings.isNullOrEmpty(dispatchEndAt)) {
 			LocalDateTime endTime = LocalDateTime.parse(dispatchEndAt.trim() + " 00:00:00", formatter);
 			params.put("dispatchEndAt", endTime);
 		}
 		boolean flag = true;
 		Subject subject = SecurityUtils.getSubject();
 		if (subject.isPermitted("dispatchWorkOrder:branch:export")) {
-			int userId = WebUtil.currentUserId(httpServletRequest);
-	    	ServiceResult<List<SysPartyUser>> sr = sysPartyService.getPartyUserByUserId(Long.parseLong(String.valueOf(userId)));
-	    	List<String> branchList = Lists.newArrayList();
-	    	Set<String> set = Sets.newHashSet();
-	    	if(sr.getSuccess()&&sr.getResult()!=null){
-	    		List<SysPartyUser> list = sr.getResult();
-	    		list.forEach(partyUser->{
-	    			ServiceResult<SysParty> pSr = sysPartyService.getById(partyUser.getPartyId());
-	    			if(pSr.getSuccess()&&pSr.getResult()!=null){
-	    				SysParty sysParty = pSr.getResult();
-	    				if(!set.contains(sysParty.getPartyCode())) {
-	    					set.add(sysParty.getPartyCode());
-	    					branchList.add(sysParty.getPartyCode());
-	    				}
-	    			}
-	    		});
-	    	}
-	    	if(branchList.size()>0) {
-	    		params.put("branchList", branchList);
-	    	}else {
-	    		flag = false;
-	    	}
+			super.putSubCenterNew(params, httpServletRequest);
+//			int userId = WebUtil.currentUserId(httpServletRequest);
+//	    	ServiceResult<List<SysPartyUser>> sr = sysPartyService.getPartyUserByUserId(Long.parseLong(String.valueOf(userId)));
+//	    	List<String> branchList = Lists.newArrayList();
+//	    	Set<String> set = Sets.newHashSet();
+//	    	if(sr.getSuccess()&&sr.getResult()!=null){
+//	    		List<SysPartyUser> list = sr.getResult();
+//	    		list.forEach(partyUser->{
+//	    			ServiceResult<SysParty> pSr = sysPartyService.getById(partyUser.getPartyId());
+//	    			if(pSr.getSuccess()&&pSr.getResult()!=null){
+//	    				SysParty sysParty = pSr.getResult();
+//	    				if(!set.contains(sysParty.getPartyCode())) {
+//	    					set.add(sysParty.getPartyCode());
+//	    					branchList.add(sysParty.getPartyCode());
+//	    				}
+//	    			}
+//	    		});
+//	    	}
+//	    	if(branchList.size()>0) {
+//	    		params.put("branchList", branchList);
+//	    	}else {
+//	    		flag = false;
+//	    	}
 	    	
 		}
 		try {
 			Page<LightWorkOrderDto> singlePage = lightWorkOrderService.findByPage(new PageRequest(1, 5000), params);
 			// 数据不为空进行导出
 			if (!CollectionUtils.isEmpty(singlePage.getContent())) {
 				String fileName = URLEncoder.encode("派工单信息", "UTF-8");
 				httpServletResponse.setContentType("application/vnd.ms-excel");
 				httpServletResponse.setCharacterEncoding("utf-8");
 				httpServletResponse.setHeader("Content-disposition", "attachment;filename=" + fileName + ".xlsx");
 				ExcelWriter excelWriter = EasyExcel
 						.write(httpServletResponse.getOutputStream(), LightWorkOrderDtoExcel.class).build();
 				try {
 					WriteSheet writeSheet = EasyExcel.writerSheet("派工单信息").build();
 					int pageNum = 1;
 					do {
 						singlePage = lightWorkOrderService.findByPage(new PageRequest(pageNum, 5000), params);
 						if (CollectionUtils.isEmpty(singlePage.getContent())) {
 							break;
 						}
 						excelWriter.write(toExcel(singlePage.getContent()), writeSheet);
 						pageNum++;
 					} while (true);
 				} catch (Exception e) {
 					log.error("导出派工单信息写入xls异常{}", Throwables.getStackTraceAsString(e));
 				} finally {
 					if (excelWriter != null) {
 						excelWriter.finish();
 					}
 				}
 			} else {
 				// 重置response
 				httpServletResponse.reset();
 				httpServletResponse.setContentType("application/json");
 				httpServletResponse.setCharacterEncoding("utf-8");
 				ExecuteResult<Object> result = ExecuteResult.newErrorResult("无数据可以下载");
 				try {
 					httpServletResponse.getWriter().println(JSON.toJSONString(result));
 				} catch (Exception ex) {
 					throw ex;

```

### src/main/java/com/rrsjk/admin/controller/light/LightSpController.java
业务域候选: 未识别
```diff
diff --git a/src/main/java/com/rrsjk/admin/controller/light/LightSpController.java b/src/main/java/com/rrsjk/admin/controller/light/LightSpController.java
index f4cbed2f3..69180d447 100644
--- a/src/main/java/com/rrsjk/admin/controller/light/LightSpController.java
+++ b/src/main/java/com/rrsjk/admin/controller/light/LightSpController.java
@@ -154,96 +154,97 @@ public class LightSpController extends AbstractController {
         }
         if (criteria.getStatus() != null) {
             params.put("status", criteria.getStatus());
         }
         params.put("businessType", 1);
         Page<LightServiceProvider> page = lightServiceProviderService.findByPage(pageable, params);
         //无退押金权限不返回押金数据
         //        if (!currentUser.isPermitted("lightSp:refundDeposit")) {
         //            List<LightServiceProvider> list = page.getContent();
         //            list.forEach(item -> {
         //                    item.setDeposit(null);
         //                    item.setDepositBalance(null);
         //            });
         //        }
         EasyuiDataGrid<LightServiceProvider> result = this.easyuiDataGrid(page);
         return result;
     }
 
     @RequiresPermissions("lightSp:list")
     @RequestMapping("branchSpList.html")
     public String branchSpList(Model model) {
         return "light/branchSpList";
     }
 
     @RequiresPermissions("lightSp:list")
     @ResponseBody
     @RequestMapping("branchSpList.do")
     public EasyuiDataGrid<LightServiceProvider> branchSpList(LightServiceProvider criteria, HttpServletRequest request) {
         PageRequest pageable = this.handlerPagerInfo(request);
         log.info("获取分中心服务商列表：pageable:{},criteria:{}", pageable, criteria);
         Map<String, Object> params = Maps.newHashMap();
         if (!Strings.isNullOrEmpty(criteria.getName())) {
             params.put("name", criteria.getName());
         }
         if (!Strings.isNullOrEmpty(criteria.getMemberName())) {
             params.put("memberName", criteria.getMemberName());
         }
         if (criteria.getStatus() != null) {
             params.put("status", criteria.getStatus());
         }
-        int userId = WebUtil.currentUserId(request);
-        ServiceResult<List<SysPartyUser>> sr = sysPartyService.getPartyUserByUserId(Long.parseLong(String.valueOf(userId)));
-        List<String> branchList = Lists.newArrayList();
-        if (sr.getSuccess() && sr.getResult() != null) {
-            List<SysPartyUser> list = sr.getResult();
-            list.forEach(partyUser -> {
-                ServiceResult<SysParty> pSr = sysPartyService.getById(partyUser.getPartyId());
-                if (pSr.getSuccess() && pSr.getResult() != null) {
-                    SysParty sysParty = pSr.getResult();
-                    branchList.add(sysParty.getPartyCode());
-                }
-            });
-        }
-        if (!branchList.isEmpty()) {
-            params.put("branchList", branchList);
-        }
+        super.putSubCenterNew(params, request);
+//        int userId = WebUtil.currentUserId(request);
+//        ServiceResult<List<SysPartyUser>> sr = sysPartyService.getPartyUserByUserId(Long.parseLong(String.valueOf(userId)));
+//        List<String> branchList = Lists.newArrayList();
+//        if (sr.getSuccess() && sr.getResult() != null) {
+//            List<SysPartyUser> list = sr.getResult();
+//            list.forEach(partyUser -> {
+//                ServiceResult<SysParty> pSr = sysPartyService.getById(partyUser.getPartyId());
+//                if (pSr.getSuccess() && pSr.getResult() != null) {
+//                    SysParty sysParty = pSr.getResult();
+//                    branchList.add(sysParty.getPartyCode());
+//                }
+//            });
+//        }
+//        if (!branchList.isEmpty()) {
+//            params.put("branchList", branchList);
+//        }
         params.put("businessType", 1);
         Page<LightServiceProvider> page = lightServiceProviderService.findByPage(pageable, params);
         EasyuiDataGrid<LightServiceProvider> result = this.easyuiDataGrid(page);
         return result;
     }
 
     /**
      * 查询所有省信息
      *
      * @param parentId
      *
      * @return
      */
     @RequestMapping(value = {"/listYearMonth.do"}, method = {RequestMethod.GET})
     @ResponseBody
     public ExecuteResult<List<LightSpTarget>> listYearMonth(Long spId) {
         List<LightSpTarget> list = lightSpTargetService.findBySpId(spId);
         if (list == null || list.isEmpty()) {
             list = Lists.newArrayList();
             int year = LocalDate.now().getYear();
             int month = LocalDate.now().getMonthOfYear();
             for (int i = month; i <= 12; i++) {
                 LightSpTarget target = LightSpTarget.builder().year(year).month(i).spId(spId).target(null).build();
                 list.add(target);
             }
         }
         return ExecuteResult.newSuccessResult(list);
     }
 
     /**
      * 审核服务商--通过
      *
      * @param request
      * @param response
      * @param criteria
      *
      * @return
      */
     @RequiresPermissions("lightSp:audit")
     @RequestMapping(value = {"auditOk.do"}, method = {RequestMethod.POST})

```

### src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java
业务域候选: 电站生命周期
```diff
diff --git a/src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java b/src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java
index 139eef613..29c5e2b8f 100644
--- a/src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java
+++ b/src/main/java/com/rrsjk/admin/controller/light/lightStationNannyUserController.java
@@ -32,141 +32,143 @@ import org.springframework.web.bind.annotation.ResponseBody;
 import javax.servlet.http.HttpServletRequest;
 import javax.servlet.http.HttpServletResponse;
 import java.net.URLEncoder;
 import java.time.LocalDateTime;
 import java.util.ArrayList;
 import java.util.Arrays;
 import java.util.List;
 import java.util.Map;
 
 
 @Controller
 @Slf4j
 @RequestMapping("/lightStationNannyUser")
 public class lightStationNannyUserController extends AbstractController {
 
     @Autowired
     private LightStationNannyUserService lightStationNannyUserService;
 
     @Autowired
     private SysPartyService sysPartyService;
 
     @RequiresPermissions("lightStationNannyUser:read:list")
     @RequestMapping("list.html")
     public String list(Map<String, Object> dataMap) {
 
         return "light/lightStationNannyUser";
     }
 
 
     @RequiresPermissions("lightStationNannyUser:read:list")
     @ResponseBody
     @RequestMapping("doList.do")
     public EasyuiDataGrid<LightStationNannyUser> doList(HttpServletRequest request,
                                                         LightStationNannyUser lightStationNannyUser,
                                                         String createStart,
                                                         String createEnd) {
 
         PageRequest pageable = this.handlerPagerInfo(request);
 
         // 获取当前用户可以查看的分中心
-        List<String> codeList = this.getSubCenterCodeList(request);
+//        List<String> codeList = this.getSubCenterCodeList(request);
+        List<String> codeList = super.getSubCenterCodesNewVersion(request);
 
         if (CollectionUtils.isEmpty(codeList)) {
             Page<LightStationNannyUser> page = new Page<>(Lists.newArrayList(), 0);
             return this.easyuiDataGrid(page);
         }
 
         Map<String, Object> params = this.requestToMap(lightStationNannyUser);
 
         params.put("subCenterCodeList", codeList);
 
         if (StringUtils.isNotBlank(createStart)) {
             LocalDateTime startTime = parseDateTime(createStart.trim() + " 00:00:00");
             params.put("createStart", startTime);
         }
 
         if (StringUtils.isNotBlank(createEnd)) {
             LocalDateTime endTime = parseDateTime(createEnd.trim() + " 23:59:59");
             params.put("createEnd", endTime);
         }
 
         Page<LightStationNannyUser> page = lightStationNannyUserService.findBy(pageable, params);
 
         return this.easyuiDataGrid(page);
 
     }
 
     private List<String> getSubCenterCodeList(HttpServletRequest request) {
         int userId = WebUtil.currentUserId(request);
         ServiceResult<List<SysPartyUser>> sr = sysPartyService.getPartyUserByUserId(Long.parseLong(String.valueOf(userId)));
         List<String> codeList = Lists.newArrayList();
         if (sr.getSuccess() && sr.getResult() != null) {
             List<SysPartyUser> list = sr.getResult();
             list.forEach(partyUser -> {
                 ServiceResult<SysParty> pSr = sysPartyService.getById(partyUser.getPartyId());
                 if (pSr.getSuccess() && pSr.getResult() != null) {
                     SysParty sysParty = pSr.getResult();
                     if (!codeList.contains(sysParty.getPartyCode())) {
                         codeList.add(sysParty.getPartyCode());
                     }
                 }
             });
         }
         return codeList;
     }
 
     @AcceptLog
     @RequiresPermissions("lightStationNannyUser:read:export")
     @ResponseBody
     @RequestMapping("doExport.do")
     public void doExport(LightStationNannyUser lightStationNannyUser,
                          String createStart,
                          String createEnd,
                          HttpServletRequest httpServletRequest,
                          HttpServletResponse httpServletResponse) {
 
 
         Map<String, Object> params = this.requestToMap(lightStationNannyUser);
 
         // 获取当前用户可以查看的分中心
-        List<String> codeList = this.getSubCenterCodeList(httpServletRequest);
+//        List<String> codeList = this.getSubCenterCodeList(httpServletRequest);
+        List<String> codeList = super.getSubCenterCodesNewVersion(httpServletRequest);
         if (CollectionUtils.isNotEmpty(codeList)) {
             params.put("subCenterCodeList", codeList);
         } else {
             params.put("subCenterCode", "xxxx");
         }
 
         if (StringUtils.isNotBlank(createStart)) {
             LocalDateTime startTime = parseDateTime(createStart.trim() + " 00:00:00");
             params.put("createStart", startTime);
         }
 
         if (StringUtils.isNotBlank(createEnd)) {
             LocalDateTime endTime = parseDateTime(createEnd.trim() + " 23:59:59");
             params.put("createEnd", endTime);
         }
         try {
             String fileName = URLEncoder.encode("绿能站长屋顶推荐", "UTF-8");
             httpServletResponse.setContentType("application/vnd.ms-excel");
             httpServletResponse.setCharacterEncoding("utf-8");
             httpServletResponse.setHeader("Content-disposition", "attachment;filename=" + fileName + ".xlsx");
             ExcelWriter excelWriter = EasyExcel.write(httpServletResponse.getOutputStream(), LightStationNannyUserExcel.class)
                     .build();
 
             try {
                 WriteSheet writeSheet = EasyExcel.writerSheet("绿能站长屋顶推荐").build();
                 Integer totalPages = 1;
                 for (int i = 1; i <= totalPages; i++) {
                     PageRequest pageable = new PageRequest(i, 3000);
                     Page<LightStationNannyUser> page = lightStationNannyUserService.findBy(pageable, params);
                     if (page != null) {
                         totalPages = page.getTotalPages();
                         excelWriter.write(toExcelDto(page), writeSheet);
                     }
                 }
             } catch (Exception e) {
                 log.error("导出绿能站长屋顶推荐数据异常{}", Throwables.getStackTraceAsString(e));
             } finally {
                 excelWriter.finish();
             }
         } catch (Exception e) {

```

---
## 仓库: rrsjk-light-report-service
路径: /data/pvcode/rrsjk-light-report-service
分支: master
范围: a99ff7e9541cb18a1226fa45936fb18ca7681c9b..4bdaa3c557806b83b43752852807fd995003b9b3

### Commit 和文件变更
```text
4bdaa3c557806b83b43752852807fd995003b9b3|yumiao|2026-06-07 15:48:36 +0800|fix:报表分中心

M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/ads/ElecWarningStation.xml
414ef7a28046909a2105c68ea1b5cb3fd593bc16|yumiao|2026-06-07 15:43:46 +0800|fix:报表分中心

M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/ads/ElecWarningStation.xml
M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml
995aa0e76717fb33d5605d4ff4ccaec305a586d0|yumiao|2026-06-07 12:12:02 +0800|fix:手机报表分中心

M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
3ae2cbc8278a6072cf30db0b11c932543efa054a|yumiao|2026-06-07 12:08:31 +0800|fix:手机报表分中心

M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
f54f9bb41cb1eedf101eaf8ae7ed77746277b0bc|yumiao|2026-06-07 12:03:06 +0800|fix:手机报表分中心

M	rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml

```

### 变更统计
```text
 .../main/resources/mybatis/mapper/light/LightStation.xml   | 14 +++++++-------
 .../mybatis/mapper/light/LightStationPlanConfig.xml        |  4 ++--
 2 files changed, 9 insertions(+), 9 deletions(-)

```

### 关键业务文件 diff

### rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
业务域候选: 电站生命周期
```diff
diff --git a/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml b/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
index 48194ab5..57aa3a2c 100644
--- a/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
+++ b/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStation.xml
@@ -279,135 +279,135 @@
         <foreach item="item" collection="list" separator="," close=")" open="(">
             #{item}
         </foreach>
     </select>
 
     <sql id="conditionField">
         ${field}
     </sql>
     <select id="findStationSum1" resultMap="LightStationMap" parameterType="map">
         SELECT
         sub_center_code,
         sub_center_name,
         COUNT(id) AS send_sms_sum
         FROM
         <include refid="table"/>
         WHERE status NOT IN ('DISABLE','STOP','REPURCHASE')
         and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
         AND is_test = 0
         <if test="startAt != null">
             AND<include refid="conditionField"/><![CDATA[ >= #{startAt} ]]>
         </if>
         <if test="endAt != null">
             AND<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
         </if>
         <if test="stationCodeList!=null">
             AND station_code IN
             <foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
                 #{item}
             </foreach>
         </if>
         GROUP BY sub_center_code
     </select>
 
     <select id="findStationSum" resultMap="LightStationMap" parameterType="map">
         select wa.sub_center_code,
                wa.sub_center_name,
                count(wa.id) as send_sms_sum
         from (
                 SELECT a.sub_center_code,
                        a.id,
-                       a.sub_center_name_new as sub_center_name
+                       a.sub_center_name
                 from light_station a
                 where
                     status NOT IN ('DISABLE', 'STOP')
                     AND is_test = 0
                     and stop_status IN ('INIT', 'REVOCATION', 'WAIT_AUDIT', 'AUDIT_REJECT')
                     and exists(select * from light_service_provider lsp where lsp.id = a.sp_id and (lsp.status in (6,3) or lsp.epc_status in (6,3)) )
                     <if test="startAt != null">
                     AND<include refid="conditionField"/><![CDATA[ >= #{startAt} ]]>
                     </if>
                     <if test="endAt != null">
                     AND<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
                     </if>
                     <if test="stationCodeList!=null">
                     AND station_code IN
                         <foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
                             #{item}
                         </foreach>
                     </if>
             ) wa
         where exists(select * from rrsjk_light_report.energy_summary_target se where se.sub_center = wa.sub_center_name)
         group by wa.sub_center_name
     </select>
 
     <select id="findStationSumUpgrade" resultMap="LightStationMap" parameterType="map">
         SELECT
         a.sub_center_code,
-        a.sub_center_name_new AS sub_center_name,
+        a.sub_center_name AS sub_center_name,
         COUNT(a.id) AS send_sms_sum
         FROM light_station a
         INNER JOIN light_service_provider lsp
         ON a.sp_id = lsp.id
         AND (lsp.status IN (6, 3) OR lsp.epc_status IN (6, 3))
         <where>
             a.status NOT IN ('DISABLE', 'STOP')
             AND a.is_test = 0
             AND a.stop_status IN ('INIT', 'REVOCATION', 'WAIT_AUDIT', 'AUDIT_REJECT')
             <if test="startAt != null">
                 AND a.<include refid="conditionField"/><![CDATA[ >= #{startAt} ]]>
             </if>
             <if test="endAt != null">
                 AND a.<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
             </if>
             <if test="stationCodeList!=null">
                 AND station_code IN
                 <foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
                     #{item}
                 </foreach>
             </if>
             AND EXISTS (
             SELECT 1 FROM rrsjk_light_report.energy_summary_target se
-            WHERE se.sub_center = a.sub_center_name_new
+            WHERE se.sub_center = a.sub_center_name
             )
         </where>
-        GROUP BY a.sub_center_name_new;
+        GROUP BY a.sub_center_name;
     </select>
 
     <select id="findStationRegionSum" resultMap="LightStationMap" parameterType="map">
         SELECT
         sub_center_code,
         sub_center_name,
         COUNT(DISTINCT region_id) AS send_sms_sum
         FROM
         <include refid="table"/>
         WHERE status NOT IN('DISABLE','STOP','REPURCHASE')
         and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
         AND is_test = 0
         <if test="startAt != null">
             AND<include refid="conditionField"/><![CDATA[ >= #{startAt} ]]>
         </if>
         <if test="endAt != null">
             AND<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
         </if>
         <if test="stationCodeList!=null">
             AND station_code IN
             <foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
                 #{item}
             </foreach>
         </if>
         GROUP BY sub_center_code
     </select>
     <select id="findAllStationRegionSum" resultType="Integer" parameterType="map">
         SELECT
         COUNT(DISTINCT region_id)
         FROM
         <include refid="table"/>
         WHERE status NOT IN('DISABLE','STOP','REPURCHASE')
         and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
         AND is_test = 0
         <if test="startAt != null">
             AND<include refid="conditionField"/><![CDATA[ >= #{startAt} ]]>
         </if>
         <if test="endAt != null">
             AND<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
         </if>
@@ -2397,162 +2397,162 @@
         </if>
     </select>
 
     <select id="findAssetQuota" resultType="BigDecimal" parameterType="map">
         SELECT
         SUM(s.complete_confirm_capacity) / 1000
         FROM light_station s
         <where>
             s.station_code in (
             <foreach collection="list" item="stationCode" separator=",">
                 #{stationCode}
             </foreach>)
         </where>
     </select>
 
     <select id="findAssetPlanQuota" resultType="BigDecimal" parameterType="map">
         SELECT
         SUM(s.plan_capacity) / 1000
         FROM light_station s
         <where>
             s.station_code in (
             <foreach collection="list" item="stationCode" separator=",">
                 #{stationCode}
             </foreach>)
         </where>
     </select>
 
     <select id="findTotalStationByCityIdAndTimeAt" resultType="Integer" parameterType="map">
         select count(1) from
         <include refid="table"/>
         <where>
             status not in ('DISABLE','STOP','REPURCHASE')
             and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
             <if test="cityIdList != null and cityIdList.size()!=0">
                 and city_id in (
                 <foreach collection="cityIdList" item="cityId" separator=",">
                     #{cityId}
                 </foreach>)
             </if>
             <if test="subCenterName != null">
-                and sub_center_name_new = #{subCenterName}
+                and sub_center_name = #{subCenterName}
             </if>
             <if test="orderAt != null">
                 AND order_at is not null
             </if>
             <if test="completeApplyAt != null">
                 AND complete_apply_at is not null
             </if>
             <if test="firstThreePowerAt != null">
                 AND first_three_power_at is not null
             </if>
             <if test="firstAuditAt != null">
                 AND first_audit_at is not null
             </if>
             <if test="spId != null">
                 AND sp_id = #{spId}
             </if>
             <if test="stationCodeList !=null and stationCodeList.size() != 0">
                 AND station_code in (
                 <foreach collection="stationCodeList" item="stationCode" separator=",">
                     #{stationCode}
                 </foreach>)
             </if>
         </where>
     </select>
 
     <select id="findExpiredThrCompleteStations" resultMap="LightStationMap" parameterType="map">
         SELECT ls.*,lmo.make_at
         from light_station ls ,light_make_order lmo
         <where>
             ls.id=lmo.station_id
             AND lmo.status = 'FINISHED'
             AND ls.status NOT IN ('DISABLE','STOP','REPURCHASE')
             and ls.stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
             AND ls.order_at is NOT NULL
             AND (
             CASE
             WHEN ls.complete_apply_at is not NULL
             THEN <![CDATA[ls.complete_apply_at > #{dateTime} ]]>
             ELSE ls.complete_apply_at is NULL END
             )
             AND ls.stop_status ='INIT'
             <if test="cityIdList != null and cityIdList.size()!=0">
                 AND ls.city_id in (
                 <foreach collection="cityIdList" item="cityId" separator=",">
                     #{cityId}
                 </foreach>)
             </if>
             <if test="subCenterName != null">
-                and ls.sub_center_name_new = #{subCenterName}
+                and ls.sub_center_name = #{subCenterName}
             </if>
             AND  <![CDATA[ DATEDIFF(#{dateTime}, DATE_ADD(lmo.make_at,INTERVAL 3 DAY)) > 30 ]]>
             <if test="spId != null">
                 AND ls.sp_id = #{spId}
             </if>
         </where>
     </select>
 
     <select id="findExpiredGridStations" resultMap="LightStationMap" parameterType="map">
         SELECT ls.*
         FROM light_station ls
         <where>
             ls.status NOT IN ('DISABLE','STOP','REPURCHASE')
             and ls.stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
             AND ls.order_at is NOT NULL
             AND ls.complete_apply_at is NOT NULL
             AND ls.first_three_power_at is NULL
             AND ls.mode not in ('EPC')
             AND (
             CASE
             WHEN ls.first_three_power_at is not NULL
             THEN <![CDATA[ls.first_three_power_at > #{dateTime} ]]>
             ELSE ls.first_three_power_at is NULL END
             )
             <if test="cityIdList != null and cityIdList.size()!=0">
                 AND ls.city_id in (
                 <foreach collection="cityIdList" item="cityId" separator=",">
                     #{cityId}
                 </foreach>)
             </if>
             <if test="subCenterName != null">
-                and ls.sub_center_name_new = #{subCenterName}
+                and ls.sub_center_name = #{subCenterName}
             </if>
             <if test="startExpiredDay != null">
                 AND  <![CDATA[ DATEDIFF(#{dateTime},ls.complete_apply_at) > #{startExpiredDay}]]>
             </if>
             <if test="endExpiredDay != null">
                 AND <![CDATA[ DATEDIFF(#{dateTime},ls.complete_apply_at)  <= #{endExpiredDay} ]]>
             </if>
             <if test="spId != null">
                 AND sp_id = #{spId}
             </if>
         </where>
     </select>
 
     <select id="findListByFirstThreePower" resultType="String" parameterType="map">
         select station_code
         from
         <include refid="table"/>
         where status not in ('DISABLE','STOP','REPURCHASE')
         and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
         <if test="startTime != null">
             AND <![CDATA[ first_three_power_at >= #{startTime} ]]>
         </if>
         <if test="endTime != null">
             AND <![CDATA[ first_three_power_at < #{endTime} ]]>
         </if>
     </select>
 
     <select id="findStationListByFirstThreePower" resultMap="LightStationMap" parameterType="map">
         select id,<include refid="columns"/>
         from
         <include refid="table"/>
         where status not in ('DISABLE','STOP','REPURCHASE')
         and stop_status in ('INIT','REVOCATION','AUDIT_REJECT')
         <if test="startTime != null">
             AND <![CDATA[ first_three_power_at >= #{startTime} ]]>
         </if>
         <if test="endTime != null">
             AND <![CDATA[ first_three_power_at < #{endTime} ]]>
         </if>
     </select>

```

### rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml
业务域候选: 电站生命周期
```diff
diff --git a/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml b/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml
index 56b6b3d8..3900ee66 100644
--- a/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml
+++ b/rrsjk-light-report-impl/src/main/resources/mybatis/mapper/light/LightStationPlanConfig.xml
@@ -150,81 +150,81 @@
 				AND s.<include refid="conditionField"/><![CDATA[ <= #{endAt} ]]>
 			</if>
 			<if test="type != null" >
 				AND sp.type = #{type}
 			</if>
 			<if test="stationCodeList!=null and stationCodeList.size() != 0">
 				AND s.station_code IN
 				<foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
 					#{item}
 				</foreach>
 			</if>
 		</where>
 	</sql>
 
 <!--  <select id="findStationPower" parameterType="map" resultMap="LightStationPlanConfigMap">-->
 <!--		SELECT-->
 <!--			s.sub_center_code AS sku,-->
 <!--			s.sub_center_name AS sku_name,-->
 <!--			SUM(sp.plan_quantity * sp.power) AS plan_quantity,-->
 <!--			SUM(sp.complete_confirm_quantity * sp.power) AS complete_confirm_quantity-->
 <!--		FROM light_station s, light_station_plan_config sp-->
 <!--		<include refid="reportCondition"/>-->
 <!--		GROUP BY s.sub_center_code-->
 <!--	</select>-->
 
 
 	<select id="findStationPower" parameterType="map" resultMap="LightStationPlanConfigMap">
 		SELECT s.sub_center_code AS sku,
 		s.sub_center_name AS sku_name,
 		sum(s.plan_capacity) AS plan_quantity,
 		sum(s.complete_confirm_capacity) AS complete_confirm_quantity
 		FROM (
 			select wa.sub_center_code,
 		           wa.sub_center_name,
 	               wa.id,
 	               wa.plan_capacity,
 	               wa.complete_confirm_capacity
 		    from (
 		    	SELECT a.sub_center_code,
 		    	       a.id,
-		    	       a.sub_center_name_new as sub_center_name,
+		    	       a.sub_center_name as sub_center_name,
 		               a.plan_capacity * 1000 as plan_capacity,
 		               a.complete_confirm_capacity * 1000 as complete_confirm_capacity
 		        from light_station a
 		        where
 		            a.status NOT IN ('DISABLE', 'STOP')
 		          	AND a.is_test = 0
 		          	and a.stop_status IN ('INIT', 'REVOCATION', 'WAIT_AUDIT', 'AUDIT_REJECT')
 		          	and exists(select * from light_service_provider lsp where lsp.id = a.sp_id and (lsp.status in (6,3) or lsp.epc_status in (6,3)))
 					<if test="startAt != null">
 					AND a.<include refid="conditionField"/>
 						<![CDATA[ >= #{startAt} ]]>
 					</if>
 					<if test="endAt != null">
 					AND a.<include refid="conditionField"/>
 						<![CDATA[ <= #{endAt} ]]>
 					</if>
 					<if test="stationCodeList!=null and stationCodeList.size() != 0">
 					AND a.station_code IN
 						<foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
 							#{item}
 		        		</foreach>
 					</if>
 		        ) wa
 		    where exists(select * from rrsjk_light_report.energy_summary_target se where se.sub_center = wa.sub_center_name)
 		) s
 		GROUP BY s.sub_center_name
 	  </select>
 
 	<select id="findStationPowerPro" parameterType="map" resultMap="LightStationPlanConfigMap">
 		SELECT
 		s.sub_center_code AS sku,
 		s.sub_center_name AS sku_name,
 		SUM(sp.plan_quantity * sp.power) AS plan_quantity,
 		SUM(sp.complete_confirm_quantity * sp.power) AS complete_confirm_quantity,
 		SUM(sp.acceptance_confirm_quantity * sp.power) AS acceptance_confirm_quantity
 		FROM light_station s, light_station_plan_config sp
 		<include refid="reportConditionPro"/>
 		GROUP BY s.sub_center_code
 	</select>
 
@@ -246,71 +246,71 @@
 		AND status in('INIT','COMPLETE','ACCEPTANCE')
 	</select>
 
 	<select id="findByParams" resultMap="LightStationPlanConfigMap" parameterType="map">
 		SELECT id,
 		<include refid="columns"/>
 		FROM
 		<include refid="table"/>
 		<include refid="condition"/>
 	</select>
 
     <select id="findQuotaByStationCodeList" resultType="BigDecimal" parameterType="map">
         SELECT
         SUM(pc.power*pc.complete_confirm_quantity)/1000
         FROM light_station s, light_station_plan_config pc
         <where>
             s.id = pc.station_id
             AND pc.status IN('INIT','COMPLETE','ACCEPTANCE')
             AND pc.type = 'MODULE'
 			AND s.station_code IN
 			<foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
 				#{item}
 			</foreach>
         </where>
     </select>
 
 
 	<select id="findStationGroupPower" parameterType="map" resultMap="LightStationPlanConfigMap">
 		SELECT 	s.id as station_id,
 				sum(s.plan_capacity) AS plan_quantity,
 				sum(s.complete_confirm_capacity) AS complete_confirm_quantity
 		FROM (
 			select wa.sub_center_code,
 				wa.sub_center_name,
 				wa.id,
 				wa.plan_capacity,
 				wa.complete_confirm_capacity
 			from (
 				SELECT a.sub_center_code,
 					a.id,
-					a.sub_center_name_new as sub_center_name,
+					a.sub_center_name,
 					a.plan_capacity * 1000 as plan_capacity,
 					a.complete_confirm_capacity * 1000 as complete_confirm_capacity
 				from light_station a
 				where
 					a.status NOT IN ('DISABLE', 'STOP')
 					AND a.is_test = 0
 					and a.stop_status IN ('INIT', 'REVOCATION', 'WAIT_AUDIT', 'AUDIT_REJECT')
 					and exists(select * from light_service_provider lsp where lsp.id = a.sp_id and (lsp.status in (6,3) or lsp.epc_status in (6,3)))
 				<if test="startAt != null">
 					AND a.<include refid="conditionField"/>
 					<![CDATA[ >= #{startAt} ]]>
 				</if>
 				<if test="endAt != null">
 					AND a.<include refid="conditionField"/>
 					<![CDATA[ <= #{endAt} ]]>
 				</if>
 				<if test="stationCodeList!=null and stationCodeList.size() != 0">
 					AND a.station_code IN
 					<foreach collection="stationCodeList" separator="," open="(" close=")" item="item">
 					#{item}
 					</foreach>
 				</if>
 			) wa
 			where exists(select * from rrsjk_light_report.energy_summary_target se where se.sub_center = wa.sub_center_name)
 		) s
 		GROUP BY station_id
 	</select>
 
 
 </mapper>

```

---
## 仓库: rrsjk-light-service
路径: /data/pvcode/rrsjk-light-service
分支: master
范围: origin/master..7b3190746e17dbbd55f35cb1100ea4ecb368806a

### Commit 和文件变更
```text
7b3190746e17dbbd55f35cb1100ea4ecb368806a|baoxin|2026-06-06 16:12:13 +0800|fix:修改

M	rrsjk-light-impl/src/main/resources/mybatis/mapper/LightSpAuthorityZone.xml
fbc267d24cd7866a4cbfd3b1a3df8e24316e2b3a|baoxin|2026-06-06 16:10:39 +0800|Merge branch 'master' of codeup.aliyun.com:5ede241b405cdab50f4001e7/rrsjk-light/rrsjk-light-service
44cacedd1b721bccd77d9270e8c7f82f01bf9667|龙龙|2026-06-06 11:17:35 +0800|Merge remote-tracking branch 'origin/feature/202605/regionUnlimit'
9fdf456e547e72f9b85f7d5fc0b34c6fddbaf169|德|2026-06-06 10:59:11 +0800|Merge branch 'feature/202605/regionUnlimit'

```

### 变更统计
```text

```

### 关键业务文件 diff


---
## 仓库: rrsjk-merchant-h5
路径: /data/pvcode/rrsjk-merchant-h5
分支: master
范围: origin/master..9a35d0ff100f00149f8bee392a112130d7f1fb54

### Commit 和文件变更
```text
9a35d0ff100f00149f8bee392a112130d7f1fb54|李宁|2026-06-06 15:52:27 +0800|Merge #656 into master from dev-yrl-fixPreviewModal
ccf200799fdf9b086d48c7187c642dbbbd7c4782|李宁|2026-06-06 15:52:12 +0800|Merge #655 into master from dev-yrl-national

```

### 变更统计
```text

```

### 关键业务文件 diff


---
## 仓库: rrsjk-merchant-web
路径: /data/pvcode/rrsjk-merchant-web
分支: master
范围: origin/master..9b2b36304250031afff2f15efd18608d3da7ebaf

### Commit 和文件变更
```text
9b2b36304250031afff2f15efd18608d3da7ebaf|baoxin|2026-06-06 16:01:14 +0800|Merge branch 'feature/202605/regionUnlimit'

```

### 变更统计
```text

```

### 关键业务文件 diff
